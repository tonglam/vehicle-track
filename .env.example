# Vehicle Track (Option A: Supabase Postgres only)
#
# Cursor tooling may block writing .env* directly.
# Rename this file to `.env.local` in the project root and fill in real values.
# Never commit real credentials.
#
# WHERE TO FIND THESE VALUES:
# Supabase Dashboard → Settings (⚙️) → Database (NOT API!) → Connection string
# - Use "Transaction mode" (port 6543, pooler) for SUPABASE_DATABASE_URL
# - Use "Session mode" (port 5432, direct) for SUPABASE_DIRECT_URL
#
# IMPORTANT: You need the POSTGRES connection strings, NOT the Project API URL!
# ✅ Correct: postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# ❌ Wrong:   https://[project-ref].supabase.co (this is Project API, not database)

# Supabase Postgres pooled connection string (recommended for serverless runtimes)
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
SUPABASE_DATABASE_URL=""

# Supabase Postgres direct connection string (useful for migrations/admin scripts)
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
SUPABASE_DIRECT_URL=""
